# 判断列表中的元素在字符串中
def is_field_in_list(source_data, field_list):
    for field in field_list:
        if field in source_data:
            return True
    return False


def Judge_province(info):
    province_dict = {

    }
